/**
 * Basic (Configuration and Command-line) options for butler's command.
 */
export interface BasicOptions {
  /**
   * Show verbose output.
   *
   * @default false
   */
  verbose?: boolean
  /**
   * Dry run.
   *
   * @default false
   */
  dryRun?: boolean
}
