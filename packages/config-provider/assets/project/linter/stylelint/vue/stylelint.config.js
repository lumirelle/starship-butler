import lumirelle from '@lumirelle/stylelint-config'

export default lumirelle({
  /**
   * If you prefer using `prettier` to format your styles, set this to `false`.
   *
   * If you are using my eslint config with `formmaters: true`, notice that will enable prettier for styles as well.
   */
  stylistic: true,
})
