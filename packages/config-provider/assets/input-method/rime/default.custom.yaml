# After you applying this custom configuration,
# don't forget to "deploy" Rime and wait for few seconds/minutes.
patch:
  key_binder/bindings/+:
    - {when: always, accept: Release+Escape, toggle: ascii_mode}
