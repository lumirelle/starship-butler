declare module '@lumirelle/stylelint-config' {
  /**
   * Mocked exported `lumirelle` function from `@lumirelle/stylelint-config` for placeholder purpose.
   */
  export function lumirelle(): Record<string, any>
}
