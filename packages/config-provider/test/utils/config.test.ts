import { describe, expect, it } from 'bun:test'

describe('config utilities', () => {
  // TODO(Lumirelle): Add actual tests
  it('should pass a sample test', () => {
    expect(1 + 1).toBe(2)
  })
})
