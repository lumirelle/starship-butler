[env]
_.path = [ "./node_modules/.bin" ]

[tasks.dev]
run = "tsdown --watch"

[tasks.start]
run = "bun run ./src/cli.ts"

[tasks.build]
run = "tsdown --publint --attw.profile esm-only"

[tasks.typecheck]
run = "tsc --noEmit"
